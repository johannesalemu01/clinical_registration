import 'package:flutter/material.dart';

class ViewLogBook extends StatelessWidget {
  const ViewLogBook({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title: const Text('ViewLogBook'),
      ),
    );
  }
}