import 'package:flutter/material.dart';

class DownloadLogBook extends StatelessWidget {
  const DownloadLogBook({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('DownloadLogBook'),
      ),
    );
  }
}
